nextflow_pipeline {

    name "Test Pipeline with 1 process"
    script "main.nf"

    test("Should run without failures") {

        when {
            params {
              outdir = "results/test"
            }
        }

        then {
            assert workflow.success
        }

    }

}